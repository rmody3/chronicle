<%= form_for(@account, url: sessions_path) do |f| %>
  <%= f.label :email %>
  <%= f.text_field :email %>

  <%= f.label :password %>
  <%= f.password_field :password %>

  <%= f.submit :Login%>
<% end %>
<p>New user? <%= link_to "Sign up now", new_account_path %></p>
