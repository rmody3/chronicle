<div class="owl-carousel owl-theme">
  <% chronicle.articles.each do |article| %>
    <div class="item">
      <div class="articleContainer">
        <% if article.pic_url.blank? %>
          <%= image_tag("article_default", size: "400x240") %>
        <% else %>
          <%= image_tag(article.pic_url, size: "400x240") %>
        <% end %>
        <strong id="articleInfo"><%= link_to article.title, article.url, target: :_blank, class: "articleLink"%><br /><%= article.date %></strong>
      </div>
    </div>
  <% end %>
</div>
